<!DOCTYPE html>
<html>
<head>
    <title>Chess Message Game</title>
    <link rel="stylesheet" href="https://unpkg.com/chessboardjs@1.0.0/dist/chessboard-1.0.0.min.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://unpkg.com/chessboardjs@1.0.0/dist/chessboard-1.0.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"></script>

    <style>
        body { font-family: Arial, sans-serif; text-align: center; margin-top: 50px; }
        #board { width: 400px; margin: 20px auto; }
        #message { 
            display:none; position:fixed; top:50%; left:50%; 
            transform:translate(-50%,-50%);
            background:white; border:2px solid #333; 
            padding:20px; font-size:18px; z-index:1000; 
        }
        #overlay { 
            display:none; position:fixed; top:0; left:0; 
            width:100%; height:100%; 
            background:rgba(0,0,0,0.5); z-index:500; 
        }
    </style>
</head>
<body>
    <h2>لعبتك الخاصة بالشطرنج</h2>

    <div id="overlay"></div>
    <div id="message">🎉 هذه رسالتك الخاصة! 🎉</div>
    <div id="board"></div>

    <script>
        var board;
        var game = new Chess();
        var messageShown = false;

        function onDragStart(source, piece, position, orientation) {
            if(messageShown) return false; // تمنع الحركة بعد ظهور الرسالة
        }

        function onDrop(source, target) {
            var move = game.move({ 
                from: source, 
                to: target, 
                promotion: 'q' 
            });

            if(move === null) return 'snapback';

            if(!messageShown){
                messageShown = true;
                $('#overlay').show();
                $('#message').show();

                setTimeout(() => {
                    $('#overlay').hide();
                    $('#message').hide();
                }, 5000); // الرسالة تظهر 5 ثواني
            }
        }

        var config = {
            draggable: true,
            position: 'start',
            onDragStart: onDragStart,
            onDrop: onDrop
        };

        board = Chessboard('board', config);
    </script>
</body>
</html>